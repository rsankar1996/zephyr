description: wrapper for gpio led to add blink period property
compatible: "my,gpio-leds"

include: [gpio-leds.yaml]

properties:
  blink-period-ms:
    type: int
    required: false
    default: 500
